explain (VERBOSE, COSTS OFF) select key, COUNT (key) from test_lb group by key;
                             QUERY PLAN                             
--------------------------------------------------------------------
 Foreign Scan
   Output: key, (count(key))
   Relations: Aggregate on (public.test_lb)
   Remote SQL: SELECT key, count(key) FROM public.test GROUP BY key
(4 rows)

select key, COUNT (key) from test_lb group by key;
 key  |  count  
------+---------
 key1 |      10
 key2 | 1000000
(2 rows)

